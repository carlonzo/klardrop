CREATE TABLE devices_db (
  device_id TEXT PRIMARY KEY NOT NULL,
  type INTEGER NOT NULL,
  name TEXT NOT NULL,
  os INTEGER NOT NULL
);

CREATE INDEX device_id ON devices_db(device_id);

select_device:
    SELECT * FROM devices_db
    WHERE device_id = :device_id
    LIMIT 1;