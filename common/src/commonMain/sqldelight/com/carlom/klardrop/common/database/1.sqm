-- Migration: version 1 → 2
-- Add send_status column for final delivery state (SENT/FAILED only; SENDING is memory-only).
-- NULL = SENT (default for all existing rows and new successful sends), 'FAILED' = failed send.
ALTER TABLE messages ADD COLUMN send_status TEXT DEFAULT NULL;
