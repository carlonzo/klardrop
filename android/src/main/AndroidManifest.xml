<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Large outgoing file transfers run in a foreground service so they survive the share
         sheet closing and aren't killed mid-transfer; POST_NOTIFICATIONS lets it show progress. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- WifiLock during a file transfer keeps WiFi at full power so power-save doesn't drop it. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Opt-in "stay discoverable" foreground service keeps the app reachable in the background. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"/>
    <!-- MulticastLock so mDNS discovery/announcements keep flowing while backgrounded. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>

    <!-- Reading the gallery for the attachment chooser's inline recent-media rail.
         Granular media perms on Android 13+, VISUAL_USER_SELECTED for Android 14
         "selected photos" partial access; legacy storage read below API 33. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32"/>

    <application
            android:name=".KlarDropApplication"
            android:label="Klardrop"
            android:allowBackup="false"
            android:supportsRtl="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <meta-data android:name="com.bugsnag.android.API_KEY" android:value="3e6d40359747c7552a4dd9bdd45ddf16"/>
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".share.ShareToDeviceActivity"
                android:exported="true"
                android:theme="@style/Theme.Klardrop.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>
            </intent-filter>
        </activity>

        <service
                android:name=".share.FileSendService"
                android:exported="false"
                android:foregroundServiceType="dataSync"/>

        <service
                android:name=".service.DiscoveryForegroundService"
                android:exported="false"
                android:foregroundServiceType="connectedDevice"/>

        <!-- FileProvider used by InternalPlatformDependencies.openFile to hand non-content
             URIs (regular files saved to Downloads / cache / files) to viewer apps. Image
             and video receives go through MediaStore and use content:// URIs directly,
             bypassing this provider — but it's still needed for PDFs, docs, etc. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>
</manifest>